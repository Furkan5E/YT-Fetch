[project]
name = "yt-fetch"
version = "1.0.0"
description = "A interactive command line interface (CLI) for downloading YouTube videos and audio. Built with Python and yt-dlp."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "yt-dlp>=2026.8.19",
]
